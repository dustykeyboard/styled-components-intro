import { ThemeProvider } from 'styled-components';
import StyledButton, {buttonStyle} from 'src/components/StyledButton';
import ThemedButton from 'src/components/ThemedButton';
import ThemedContainer from 'src/components/ThemedContainer';
import defaultTheme from 'src/themes';
import darkTheme from 'src/themes/dark';
import { hack, syntaxHighlighterPrism } from 'mdx-deck/themes';

export const themes = [hack, syntaxHighlighterPrism];

# Theming
# `<StyledComponents/>`

// Philip Boardman

---

## `<button/>`

```jsx
<button>Hello, button</button>
```

---

<button>Hello, button</button>

---

## CSS `<button/>`

```css
button {
  color: #f8f0f8;
  background: #8a24c2;
  padding: 0.5em;
  border: 0;
  border-radius: 0.25em;
}
```

```html
<button>Hello, CSS</button>
```

---

<button style={{
  fontSize: '48px',
  color: '#f8f0f8',
  background: '#8a24c2',
  padding: '0.5em',
  border: 0,
  borderRadius: '0.25em',
}}>Hello, CSS</button>

---

## CSS in JS `<button />`

```jsx
<button style={{
  color: '#f8f0f8',
  background: '#8a24c2',
  padding: '0.5em',
  border: 0,
  borderRadius: '0.25em',
}}>Hello, CSS in JS</button>
```

---

<button style={{
  fontSize: '48px',
  color: '#f8f0f8',
  background: '#8a24c2',
  padding: '0.5em',
  border: 0,
  borderRadius: '0.25em',
}}>Hello, CSS in JS</button>

---

## Styled Component CSS

```js
import { css } from 'styled-components';

const buttonStyle = styled.css`
  color: #f8f0f8;
  background: #8a24c2;
  padding: 0.5em;
  border: 0;
  border-radius: 0.25em;
`;
```

---

## Styled Component `<button>`

```jsx
import styled from 'styled-components';

const StyledButton = styled.button`
  color: #f8f0f8;
  background: #8a24c2;
  ...
  $​{buttonStyle}
`;
```

---

## `<StyledButton/>`

```jsx
<StyledButton>
  Hello, StyledButton
</StyledButton>
```

---

<StyledButton>Hello, StyledButton</StyledButton>

---

## A Simple Theme

```js
const defaultTheme = {
  colors: {
    bg: '#f8f0f8',
    text: '#101',
    brand: '#8a24c2',
    label: '#f8f0f8',
  }
};
export default defaultTheme;
```

---

## `<ThemedButton/>`

```jsx
const ThemedButton = styled(StyledButton)`
  color: $​{p => p.theme.colors.label};
  background: $​{p => p.theme.colors.brand};
`;

export default ThemedButton;
```

---

## Theming a `<ThemedButton/>`

```jsx
import theme from 'src/themes';

<ThemeProvider theme={theme}>
  <p>A light theme</p>
  <ThemedButton>
    Hello, ThemedButton
  </ThemedButton>
</ThemeProvider>
```

---

<ThemeProvider theme={defaultTheme}>
  <ThemedContainer>
    <p>A light theme</p>
    <ThemedButton>
      Hello, ThemedButton
    </ThemedButton>
  </ThemedContainer>
</ThemeProvider>

---

## A Dark Theme

```js
const darkTheme = {
  colors: {
    bg: '#101',
    text: '#cebbce',
    brand: '#8a24c2',
    label: '#101',
  }
};
export default darkTheme;
```

---

## A Dark Themed `<ThemedButton/>`

```jsx
import darkTheme from 'src/themes/dark';

<ThemeProvider theme={darkTheme}>
  <p>A dark theme</p>
  <ThemedButton>
    Hello, Darkness
  </ThemedButton>
</ThemeProvider>
```

---

<ThemeProvider theme={darkTheme}>
  <ThemedContainer>
    <p>A dark theme</p>
    <ThemedButton>
      Hello, Darkness
    </ThemedButton>
  </ThemedContainer>
</ThemeProvider>

---

Philip Boardman
* Twitter: [@pbrdmn](https://twitter.com/pbrdmn)
* GitHub: [/dustykeyboard](https://github.com/dustykeyboard)
* Slides: [/styled-components-intro](https://github.com/dustykeyboard/styled-components-intro)
